Auth — 88%
Работает стабильно, есть валидации, cookie/auth-flow, защита superadmin.
Минус: часть смежных тестов бьется не из-за auth-логики, а из-за старых тест-фикстур.

Организация / участники / инвайты — 85%
Функционал хороший, проверки на membership есть, UI в обычной админке улучшен.
Плюс добавлена пред-валидация перед отправкой invite email в celery-task.

Доступ / RBAC — 82%
Базовые правила и ограничения работают, тесты проходят.

Таблицы — 58%
По факту модуль развит, но тестовый контур сейчас красный (много падений).
Для релиза блокер до стабилизации тестов и лимитов/валидаций в спорных местах.

Отчеты / Dashboard — 72%
Есть CRUD виджетов, AI-конструктор, модалка выбора таблиц.
Но backend тесты модуля сейчас падают, значит релизная готовность ниже желаемой.

AI — 70%
Логика и лимиты есть, но тесты частично красные.
Нужно добить тестовый контур и еще раз пройти edge-case по лимитам/ошибкам провайдера.

Расписание — 65%
Функционал есть, но тесты модуля сейчас падают.
До прод-готовности нужно закрыть сценарии и regression по уведомлениям.

Биллинг — 62%
Много сделано по lifecycle, grace/freeze/purge, но сейчас тесты модуля красные.
Для релиза — обязательная стабилизация и повторная верификация бизнес-правил.

Файлы — 68%
Есть upload/download, но тест показывает runtime edge-case в тестовом storage-mock.
Нужно выровнять контракт хранилища и тестовый двойник.

Уведомления — 80%
Базовый модуль рабочий, тесты зеленые, invite email теперь с pre-check.
Остается расширять наблюдаемость (метрики/алерты).

Аудит — 84%
Работает и тестируется, хорошая база для security-контроля.

Superadmin — 66%
UI/функции существенно продвинуты, но тесты частично падают (auth ожидания, login helper).
Нужно стабилизировать API-контракты и тесты.

Frontend (пользовательская часть) — 78%
Сильно улучшен UX, но нет автотестов и есть риск регрессий при быстрых правках.

Frontend (админка org) — 76%
Работает, улучшена обработка invite-сценариев.
Нужно добавить e2e и унифицировать поведение с MembersPage.

Инфра / фоновые задачи (Celery/RabbitMQ/Compose) — 74%
Контейнеры поднимаются стабильно, worker/beat живы.
Остаются риски по общей релизной воспроизводимости и части CI-checks.

Общая готовность проекта к релизу сейчас: ~72%.
Если ориентир именно “прод без сюрпризов”, то блокер №1 — довести тестовый контур до зеленого (27 падений убрать), после этого оценка поднимется до ~85%+.


Убрать N + 1 запросы со всех модулей